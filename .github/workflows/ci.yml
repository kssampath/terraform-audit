name: CI

on:
  push:
    branches:
      - main
      - test

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: hello
        run: echo "Hello, world!"